name: biomni_e1
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - blast
  - samtools
  - bowtie2
  - bwa
  - bedtools
  - fastqc
  - trimmomatic
  - mafft
  - gseapy
  - mageck
  - plannotate
  - pip:
    - scanpy
    - biopython
    - biopandas
    - biotite
    - opencv-python
    - rdkit
    - macs2
    - pymc3
    - cryosparc-tools
    - lifelines
    - scikit-image
    - googlesearch-python
    - PyPDF2
    - gget
    - faiss-cpu
    - cellxgene-census
    - scikit-bio
    - pymed
    - arxiv
    - scholarly
    - umap-learn
    - scrublet
    - scvelo
    # EXCLUDED for Mac ARM64 build (requires scikit-misc which needs Fortran):
    # - scvi-tools
    - harmony-pytorch
    - pyliftover
    - pytdc==0.4.0
    - pysam
    - pyfaidx
    # EXCLUDED for Mac ARM64 build (wheel build failures - pyranges depends on pybedtools):
    # - pyranges
    # - pybedtools
    # EXCLUDED for Mac ARM64 build (no Python 3.11 wheels):
    # - openmm
    # - pystan
    - igraph
    - pyscenic
    - cooler
    - trackpy
    - flowcytometrytools
    - cellpose
    - viennarna
    - PyMassSpec
    - python-libsbml
    - cobra
    - reportlab
    #- flowkit
    - hmmlearn
    # EXCLUDED for Mac ARM64 build (wheel build failures):
    # - msprime
    - tskit
    - cyvcf2
    - pykalman
    - community
    # EXCLUDED for Mac ARM64 build (fanc depends on pybedtools which fails to build):
    # - fanc
