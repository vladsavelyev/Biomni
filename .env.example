# Example environment configuration for MCP Biomni Docker deployment
#
# NOTE: When using with Navari, DO NOT create a local .env file here.
# Instead, add these variables to the root Navari .env file at /Users/kbww511/git/navari/.env
#
# For standalone deployment, copy this to .env and customize for your setup

# =============================================================================
# REQUIRED: Biomni Data Path
# =============================================================================
# Path to your Biomni data directory on the host machine
# This will be mounted into containers at /app/data
DATA_DIR=./data

# =============================================================================
# REQUIRED: API Keys (choose at least one)
# =============================================================================
# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (recommended for Biomni)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Azure OpenAI API Key
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here

# Custom API configuration (for OpenRouter, etc.)
BIOMNI_CUSTOM_BASE_URL=your_custom_base_url_here
BIOMNI_CUSTOM_API_KEY=your_custom_api_key_here

# =============================================================================
# Biomni Configuration
# =============================================================================
# Default LLM model for Biomni
BIOMNI_LLM=model_name_here

# Timeout for Biomni operations (seconds)
BIOMNI_TIMEOUT_SECONDS=1200

# Enable tool retriever for better context handling
BIOMNI_USE_TOOL_RETRIEVER=true

# =============================================================================
# MCP Server Configuration
# =============================================================================
# Host interface for servers (0.0.0.0 for all interfaces)
MCP_SERVER_HOST=0.0.0.0

# Base port for MCP servers (default: 8001)
MCP_BASE_PORT=8001

# Port range end for different deployment modes
MCP_BASE_PORT_END=8020      # For single container (all modules)
MCP_CORE_PORT_END=8005      # For core modules only

# =============================================================================
# Wrapper Service Configuration (for Navari integration)
# =============================================================================
# Workspace directory for Biomni agent results
WORKSPACE_DIR=/workspace

# Biomni MCP Server Configuration (JSON format)
# Maps MCP server names to their URLs
BIOMNI_MCP_SERVERS_JSON={"data_lake": "http://mcp-biomni-all:8003/mcp/", "biochemistry": "http://mcp-biomni-all:8001/mcp/", "genetics": "http://mcp-biomni-all:8002/mcp/"}

# Enable verbose logging for biomni-agent (default: false)
MODEL_VERBOSE=false

# =============================================================================
# Docker Configuration
# =============================================================================
# Compose project name (optional)
COMPOSE_PROJECT_NAME=mcp-biomni

# Docker build arguments (optional)
DOCKER_BUILDKIT=1
